# 로컬 복사: cp .env.local.example .env.local  후 값 채우기 (이 파일에는 비밀이 없도록 비워 두세요.)

## 베타 신청(/join) — Google Sheets Web App(URL) 등 JSON POST 수신 주소 (선택)
## 예: Apps Script 배포 후 나온 실행 URL을 여기 넣음. 미설정이면 저장 없이 데모만 동작.
NEXT_PUBLIC_BETA_SIGNUP_ENDPOINT=
