# Sensora Auto CRM — Claude Code 협업 지침서

## 0. 이 문서의 용도
- 이 파일을 프로젝트 루트에 `CLAUDE.md`로 저장하면, Claude Code가 세션 시작 시 자동으로 불러와 따릅니다.
- 새 세션을 시작할 때 이 내용을 직접 붙여넣어도 동일하게 작동합니다.
- 작성자(비개발자)와 Claude(claude.ai)가 함께 정리한 운영 원칙이며, 임의로 수정하지 말고 변경이 필요하면 작업자에게 먼저 보고할 것.

---

## 1. 프로젝트 개요
- **제품:** Sensora Auto CRM — 자동차 영업사원을 위한 AI 고객관리 SaaS (Sensora, B2B AI SaaS, 슬로건 "작은 시작에서부터 시작된다")
- **스택:** Next.js 16.2.4 / React 19.2.4 / Firebase 12.x (Firestore, Auth) / Tailwind CSS 4 / TypeScript
- **배포:** Vercel (Git 기반 자동 배포) — 저장소: GitHub
- **베타 기능:** `/sensora/workspace` Lead 접수
  - Firestore 경로: `sensoraWorkspaces/sensora-beta-workspace/leads/{leadId}`
  - 현재 정책: Lead 수정·삭제 비허용 (접수·조회만 가능)
- **작업자:** 비개발자, Claude Max 구독자. AI를 함께 배우며 아이디어 고도화 및 스타트업 실행을 목표로 함.

---

## 2. 핵심 운영 원칙 (모든 작업에 공통 적용)

1. **객관적이고 아첨 없는 답변.** 근거 없는 낙관적 평가나 과장된 칭찬을 하지 말 것.
2. **사실관계가 필요한 부분은 검증할 것.** 라이브러리 버전, 보안 권고, API 사양, 시장 데이터 등은 추측하지 말고 공식 문서·신뢰할 수 있는 출처를 확인한 뒤 출처를 함께 제시.
3. **작업자가 틀렸을 때는 명확히 지적할 것.** 무엇이, 왜 틀렸는지 구체적 근거와 함께 설명하고 개선 방향을 제시. 단, 근거가 명확하지 않으면 단정짓지 말고 "확실하지 않다"고 밝힐 것.
4. **결제 유도, 과장, 거짓 정보 제공 금지.** 작업자의 의사결정을 왜곡할 수 있는 어떤 형태의 유도도 하지 않을 것.
5. **비즈니스/제품 판단도 동일한 기준 적용.** 시장 규모, 기능 우선순위, 성장 가능성 등을 평가할 때도 희망적 관측이 아니라 데이터와 근거에 기반해 말할 것.

---

## 3. 권한 모드 (중요)

- **기본값:** `--permission-mode auto` 사용.
- `--dangerously-skip-permissions`는 **사용 금지** — 격리된 컨테이너/VM 환경이 아닌 한 호스트 PC에서 절대 사용하지 말 것.
- **매 세션 종료 시 다음을 요약해서 보고할 것:**
  - 변경·생성·삭제된 파일 목록
  - 실행한 셸 명령어
  - 외부로 나간 네트워크 요청(API 호출, MCP 호출 포함)
  - 권한이 거부되었거나 차단된 시도가 있었는지 여부
- 위 요약은 작업자가 별도로 claude.ai의 Claude에게 검토를 요청할 수 있으므로, 빠짐없이 기록할 것.

---

## 4. 모델 선택 기준

| 작업 유형 | 모델 | 이유 |
|---|---|---|
| 파일 구조 탐색, 단순 조회, 문서 읽기 | Haiku | 비용·속도 효율 |
| 일상적인 컴포넌트 수정, CSS, 일반 버그 수정 | Sonnet | 비용 대비 충분한 성능 |
| Firestore 보안 규칙 검토, 인증/권한 로직, 배포 설정, 복잡한 디버깅 | Opus | 보안·아키텍처 수준의 추론 필요 |

애매한 경우 작업자에게 먼저 물어볼 것. 비용을 핑계로 보안 관련 작업의 모델 등급을 낮추지 말 것.

---

## 5. MCP 사용 정책

**연결 우선순위:** Context7 → Vercel MCP → Firebase MCP → GitHub MCP

- **Context7** (문서 조회 전용, 계정 데이터 접근 없음): 위험도 낮음. Next.js 16, Firebase SDK 등 최신 버전 문서 확인 시 우선 사용.
- **Vercel MCP** (공식, 읽기 전용 권한으로 운영): 배포 로그·프로젝트 상태 확인용. 위험도 낮음.
- **Firebase MCP** (공식, Firestore 쓰기·삭제 권한 보유): 위험도 높음.
  - Firestore에 쓰기/삭제가 발생하는 작업은 auto 모드에서도 **별도로 명시적 확인**을 요청할 것.
  - 프로덕션 데이터에 영향을 주는 작업은 먼저 무엇을 할 것인지 설명하고 작업자 동의를 받은 뒤 실행할 것.
- **GitHub MCP**: 커밋·PR 생성 전 변경 내용을 요약해서 보고. `force-push`, 브랜치 강제 삭제 등 되돌리기 어려운 작업은 사전 확인 필수.

---

## 6. 보안 관련 특이사항 (이 프로젝트 고유)

- `firestore.rules`: `sellerProfiles/{uid}.approvalStatus == 'approved'`인 사용자만 본인 `createdByUid`와 일치하는 Lead에 접근 가능하도록 제한되어 있음. **임의로 권한 범위를 넓히지 말 것.**
- 베타 단계 정책상 Lead **수정·삭제는 비허용**. 이 정책을 변경해야 할 필요가 있다면 반드시 먼저 작업자에게 보고하고 동의를 받을 것.
- `.env.local`, Firebase 서비스 계정 키 등 비밀 값은 **절대 커밋 금지**. 커밋 전 `.gitignore` 적용 범위를 확인할 것.
- 환경변수가 없거나 인증·권한이 충족되지 않을 경우, 화면에 원인을 표시하고 입력값은 유지하는 기존 정책을 유지할 것.

---

## 7. 로컬 환경 관련 확인된 이슈 (2026-07-01 기준)

- **유일한 작업 폴더:** `C:\Ai\customer-manager-clean` 만 작업 폴더입니다. 같은 `C:\Ai\` 안에 `customer-manager`, `customer-manager-submit`, `sensora-backup` 등 비슷한 이름의 폴더가 더 있는데, 이들은 작업 대상이 아닙니다. 절대 혼동하지 말 것.
- **git remote 주의:** `git remote -v` 결과 `origin`(`Kor-semin/sensora-auto-crm.git`)이 정식 저장소이고, `old-ai`(`Kor-semin/AI.git`)는 용도 불명의 잔재 원격입니다. push할 때는 항상 `git push origin <브랜치명>`처럼 **원격을 명시할 것**. 원격을 생략한 `git push`는 절대 사용하지 말 것.
- **`main` 브랜치 추적 오류:** 로컬 `main`이 `origin/main`이 아니라 `old-ai/main`을 추적하도록 잘못 설정돼 있었음(미해결 가능성 있음). `main`에서 작업하기 전에 반드시 `git branch -vv`로 추적 대상을 확인하고, 필요시 `git branch --set-upstream-to=origin/main main`으로 수정할 것.
- **패키지 매니저:** 이 프로젝트는 `pnpm` 기반(`pnpm-lock.yaml`, `pnpm-workspace.yaml` 존재). `npm install`/`npm run dev` 등 npm 명령어를 절대 사용하지 말고 항상 `pnpm`을 사용할 것.
- **로컬 서버 바인딩 보안 확인 필요:** 컴퓨터 부팅 시 `next dev --hostname 0.0.0.0`이 자동 실행되도록 설정되어 있다는 보고가 있었음 — 이는 같은 네트워크의 다른 기기에서도 서버에 접근 가능하게 만듦. 이 설정이 의도된 것인지 작업자에게 확인하지 않고 임의로 변경하거나 그대로 두지 말 것.

## 8. 커뮤니케이션 방식

- 작업자는 비개발자입니다. 전문 용어를 쓸 때는 간단한 설명을 함께 제공할 것.
- 영향 범위가 큰 변경(데이터 구조 변경, 보안 규칙 수정, 배포 등) 전에는 무엇을 왜 하는지 먼저 설명하고 진행할 것.
- 긴 작업은 단계별로 나누어 중간중간 진행 상황을 보고할 것.
- 작업자가 "확인"이라고 명시적으로 답하기 전에는 되돌리기 어려운 작업(삭제, 강제 배포, 프로덕션 데이터 변경 등)을 실행하지 말 것.
